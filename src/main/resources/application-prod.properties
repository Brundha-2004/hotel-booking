# Production Configuration for Render
server.port=${PORT:8080}

# PostgreSQL Database for Production
# Automatic PostgreSQL connection from Render
# In your application-prod.properties, change to:
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://dpg-d4eqfq7pm1nc7390ldl0-a.singapore-postgres.render.com/hotelbooking_nalc}
spring.datasource.username=${JDBC_DATABASE_USERNAME:hotelbooking_nalc_user}
spring.datasource.password=${JDBC_DATABASE_PASSWORD:pbkPYQMZ1KzvxJwlZzCrMvNLvYa5gNML}

# Don't specify database name - use whatever Render provides

# JPA Configuration for PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Disable H2 Console in production
spring.h2.console.enabled=false

# Security - Enable in production
# spring.autoconfigure.exclude comment out or remove

# Logging
logging.level.com.example.hotel_booking_system=INFO
logging.level.org.springframework=WARN

# Actuator Health Checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always